# Web Framework and Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.9.0

# AI and Google Cloud Integration
# Upper bound is a deprecation stopgap: `vertexai.generative_models` (used in
# main.py) is slated for removal ~2026-06-24. v1.157.0 is confirmed to still
# ship the module, so we cap below 1.158 to keep redeploys from pulling a
# version that drops it. Proper fix = migrate to the `google-genai` SDK, then
# lift this cap.
google-cloud-aiplatform>=1.38.0,<1.158
google-auth>=2.26.0

# Utility
python-multipart==0.0.6
python-dotenv==1.0.0

# Database & Security
sqlalchemy
pg8000
cloud-sql-python-connector
bcrypt>=4.0,<6.0
PyJWT>=2.8.0