# Web Framework and Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.9.0

# AI Integration
# Chat (main.py) runs on Claude Haiku 4.5 via Anthropic on Vertex AI (billed
# through GCP, authenticated with ADC — no separate Anthropic account). The
# [vertex] extra pulls in the google-auth deps the AnthropicVertex client needs.
anthropic[vertex]>=0.40.0
# Embeddings (embeddings.py) still run on the google-genai SDK pointed at Vertex
# AI (text-embedding-004) — Anthropic has no embeddings API, so this stays. It
# also replaced the deprecated `vertexai.*` modules from google-cloud-aiplatform.
google-genai>=2.0.0
google-auth>=2.26.0

# Utility
python-multipart==0.0.6
python-dotenv==1.0.0

# Database & Security
sqlalchemy
pg8000
cloud-sql-python-connector
bcrypt>=4.0,<6.0
PyJWT>=2.8.0