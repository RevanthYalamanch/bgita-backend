# Web Framework and Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.9.0

# AI and Google Cloud Integration
# Chat (main.py) and embeddings (embeddings.py) both run on the google-genai
# SDK pointed at Vertex AI. This replaced the deprecated `vertexai.*` modules
# from google-cloud-aiplatform (slated for removal ~2026-06-24), which is no
# longer imported anywhere and has been dropped.
google-genai>=2.0.0
google-auth>=2.26.0

# Utility
python-multipart==0.0.6
python-dotenv==1.0.0

# Database & Security
sqlalchemy
pg8000
cloud-sql-python-connector
bcrypt>=4.0,<6.0
PyJWT>=2.8.0